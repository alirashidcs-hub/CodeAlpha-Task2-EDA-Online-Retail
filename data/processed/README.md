# Processed Data

`online_retail_cleaned.csv` is generated by running `src/analysis.py` (or the
notebook). It is excluded from this package/repo due to its size (~76 MB);
regenerate it locally after placing the raw dataset in `data/raw/`.
